# ── CONFIGURACIÓN LOCAL DE OLLAMA ──
# Si corrés la app fuera de Docker, usá http://localhost:11434
# Si usás Docker, el backend se conectará automáticamente al host.
OLLAMA_HOST=http://localhost:11434
MODELO_OLLAMA=gemma2:2b

# ── API KEYS (Opcional por si usás la versión de respaldo en la nube) ──
GEMINI_API_KEY=tu_clave_de_gemini_aqui
